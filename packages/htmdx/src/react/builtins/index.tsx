import type { HtmdxReactComponents } from '../index';

// All Built-ins now flow through their definitions. Keep the legacy map empty
// until the compatibility export is removed with the v4 API.
export const builtInReactComponents: HtmdxReactComponents = {};
